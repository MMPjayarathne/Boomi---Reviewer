# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# HTTP client (for Ollama calls)
httpx==0.28.1

# XML parsing
lxml==5.3.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Graph algorithms (dead path detection)
networkx==3.4.2

# Database (async SQLite)
aiosqlite==0.20.0

# AI embeddings (RAG) — downloads ~80MB model on first run
sentence-transformers==3.3.1

# Numerical operations (cosine similarity)
numpy==2.2.1

# Utilities
python-dotenv==1.0.1
